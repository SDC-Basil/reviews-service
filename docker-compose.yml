version: '3.1'
services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27107:27107
    environment:
      - MONGO_INITDB_DATABASE=Reviews

  web:
    build: .
    ports:
      - 80:3000

network: